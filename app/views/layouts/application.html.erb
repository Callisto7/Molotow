<!DOCTYPE html>
<html>
  <head>
    <title>Molotow</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
     <nav class="navbar navbar-expand-lg navbar-dark bg-light">
       <div class="container-fluid col-10 ">
       <a class="navbar-brand" href="/">RM</a>
         <button class="navbar-toggler" type="button"
                                        data-toggle="collapse"
                                        data-target="#navbarToggleExternalContent"
                                        aria-controls="navbarToggleExternalContent"
                                        aria-expanded="false"
                                        aria-label="Toggle navigation">
           <span class="navbar-toggler-icon"></span>
         </button>
         <div class="collapse navbar-collapse" id="navbarToggleExternalContent">
           <ul class="navbar-nav mr-auto">
             <li class="nav-item">
             </li>
           </ul>
           <ul class="navbar-nav pull-xs-right">
           <% if user_signed_in? %>
             <li class="nav-link btn">
               <%= link_to 'Выход', destroy_user_session_path, method: :delete, class: 'fas fa-sign-out-alt' %>
             </li>
           <% else %>
             <li class="nav-link btn ">
               <%= link_to 'Войти', new_user_session_path, class: 'fas fa-sign-in-alt' %>
             </li>
           <% end %>
           </ul>
         </div>
       </div>
     </nav>

   </div>
   <div class="container-fluid wrapper col-10">
     <div class="row">
       <div class="col-md-12">
       <% flash.each do |name, message| %>
         <div class="alert alert-<% bootstrap_class_for(name) %>success">
           <%= message %>
         </div>
       <% end %>
       </div>
     </div>
     <%= yield %>
   </div>
  </body>
</html>
